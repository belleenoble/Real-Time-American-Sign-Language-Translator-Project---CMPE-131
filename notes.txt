Just some notes if yall were curious about the code:


train_model.py
    
    Paths: Paths established at the top of the program.
    - joblib: python library for saving and loading python objects
              helps handle large numpy arrays / matrices (what we need i found out about it on google)

    -sklearn.ensemble / RandomForestClassifer: imports the classifier algorithms needed for training the train_model    
    -os.makedir("models",exist_ok=True): Creates the models in the folder if its missing
    
As of 9/16/26 we can officially train the model with the new collect_data.py and train_model.py file i updated (shoudl work for both motion and static)
 - to train the model put python collect_data.py in the terminal!
 - then go to recognize, and put `python train_model.py`

 (an error that needs to be fixed is the fact that you cant train "q" bc its the quit button. maybe if someone can change to esc)
    
TO CHECK TRAINING LOG: 
python -c "import pandas as pd; df = pd.read_csv('data/landmarks.csv'); print(df['label'].value_counts().sort_index())" 
or
python -c "import pandas as pd; df = pd.read_csv('data/motion_landmarks.csv'); print(df['label'].value_counts().sort_index())"

to push the tained joblib data: (venv active)
git pull
git add models/motion_sign_classifier.joblib (or static)
git commit -m "comment"
git push
